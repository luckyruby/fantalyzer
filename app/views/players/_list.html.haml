%hr
- if @players.present?
  %table{class: "table table-bordered table-striped table-hover table-condensed"}
    %thead
      %tr
        %th First Name
        %th Last Name
        %th Positions
        %th FPPG
        %th Games
        %th &nbsp;
    %tbody
      - @players.each do |p|
        %tr
          %td= p.first_name
          %td= p.last_name
          %td= p.positions.map(&:name).join(", ")
          %td= (p.games.map(&:fanduel).sum / p.games.length).round(1)
          %td= p.games.length
          %td= link_to 'View Games', games_player_path(p), remote: true
- else
  .no_results No Players Found
